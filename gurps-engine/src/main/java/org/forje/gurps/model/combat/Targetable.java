package org.forje.gurps.model.combat;

/**
* Identifies an entity as something which can be targeted with an attack.
 * Anything which is targetable can also implicitly take damage, potentially(?maybe?)
*/
public interface Targetable {
}
